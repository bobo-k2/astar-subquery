type Block @entity {
  id: ID! #id is a required field
  number: Int!
  date: Date
}

type StakingEra @entity {
  id: ID!
  startedAtBlock: Block!
}

type Dapp @entity {
  id: ID!
  era: StakingEra #TODO fetch current era and link
  developer: String!
}
